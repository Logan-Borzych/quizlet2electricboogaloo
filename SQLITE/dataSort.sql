-- NOTE : These are queries, not regular code. These PERMENTALLY
-- change the database. You can only run them by giving the
-- command SQLite: Run Query (Done through Ctrl + Shift + P and
-- then looking for the command through searching) Go on w3 to 
-- learn how/what these queries do. Right click flashcards.db and
-- click open database to visually see the changes. Remember 
-- to comment all queries done that have significant value so they
-- do not have to be constantly rewritten.

-- CREATE TABLE cardData(setId NUMERIC NOT NULL, cardId INTEGER NOT NULL, cardName TEXT NOT NULL, cardDef TEXT NOT NULL);

-- INSERT INTO cardData (setId, cardId, cardName, cardDef)
-- VALUES ("1", "1", "Beef", "From Cow");

-- INSERT INTO cardData (setId, cardId, cardName, cardDef)
-- VALUES ("1", "2", "Chicken", "From Chicken");

-- INSERT INTO cardData (setId, cardId, cardName, cardDef)
-- VALUES ("2", "1", "Fries", "From Taters");

-- INSERT INTO cardData (setId, cardId, cardName, cardDef)
-- VALUES ("2", "2", "Bread", "From Grain");

-- INSERT INTO cardData (setId, cardId, cardName, cardDef)
-- VALUES("3", "1", "Nate", "Very Handsome");

-- INSERT INTO cardData (setId, cardId, cardName, cardDef)
-- VALUES ("3", "2", "Sehr", "Not as Handsome");

-- UPDATE cardData
-- SET cardDef="Not Batman"
-- WHERE cardName="Sehr";

